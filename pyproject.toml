[project]
name = "highstreet-ai"
version = "0.1.0"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]  # Line length — agent prompts have long strings

[tool.ruff.lint.pycodestyle]
max-line-length = 100
